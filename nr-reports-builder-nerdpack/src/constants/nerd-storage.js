import {
  MANIFESTS_COLLECTION_NAME,
  DEFAULT_MANIFEST_FILE_NAME,
} from "../../../nr-reports-core/lib/constants"

export const NERD_STORAGE = {
  MANIFESTS_COLLECTION_NAME,
  DEFAULT_MANIFEST_FILE_NAME,
}
